public enum E_SoundType
{
    Master,
    BGM,
    SFX,
}

// 해당 enum 순서 변경을 금지합니다.
public enum E_BGM
{
    None = -1,
    BG_Test_1,
    BG_Test_2,

    Size,
}

// 해당 enum 순서 변경을 금지합니다.
public enum E_SFX
{
    None = -1,
    S_Test_1,

    Size,
}